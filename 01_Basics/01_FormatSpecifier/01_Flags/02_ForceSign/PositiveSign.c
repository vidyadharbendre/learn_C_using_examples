/*

    Program : PositiveSign.c
    Author  : Vidyadhar Bendre
    Email   : vidyadhar.bendre@gmail.com
    Description : The plus flag (+) is used to display a plus sign (+) in front of positive numbers.

*/

#include <stdio.h>

int main() {
    int positiveNumber = 200;

    // Display a plus sign for positive numbers
    printf("Positive number with plus sign: +%d\n", positiveNumber);

    return 0;
}
