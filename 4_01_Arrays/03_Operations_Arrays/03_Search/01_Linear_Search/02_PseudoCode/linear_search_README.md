### Linear Search Function - Pseudo Code

```plaintext
function linearSearch(array, target):
    for each element in the array:
        if element equals target:
            return element index
    return -1 // Element not found
